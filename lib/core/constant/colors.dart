import 'package:flutter/material.dart';

class AppColor {
  static const primary = Color(0xff143a63);
  static const secondary = Color(0xffd4a64a);
  static const background = Color(0xfff5f7fb);
  static const ink = Color(0xff172b42);
  static const muted = Color(0xff6d7b8c);

  final primaryColor = primary;
  final secondaryColor = secondary;
  final backgroundColor = background;
}
